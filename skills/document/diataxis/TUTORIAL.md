# Tutorial (learning-oriented)

- You decide what reader does; they follow. Guarantee outcome: every step must work as written, in order, from a stated starting point.
- State concrete results reader can see after each step ("the output shows `active (running)`").
- First person plural is allowed ("we now have a running service"). No options, no alternatives, no "you could also". One path.
- Minimal explanation. Link to explanation pages instead of digressing.
