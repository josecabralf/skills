# How-to guide (task-oriented)

- Title is the task: "Rotate the TLS certificate", not "TLS certificates".
- Assume competence and a working environment. No teaching, no first-principles setup.
- Sequence of actions, each step a result the reader can verify.
- Address real-world variation ("if the unit is in `blocked`, resolve that first") but stay on the task.